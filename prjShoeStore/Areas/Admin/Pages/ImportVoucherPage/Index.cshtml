@page
@model prjShoeStore.Areas.Admin.Pages.ImportVoucherPage.IndexModel

@{
    Layout = "Templates/_LayoutIndex";
    ViewData["Title"] = "Index";
    ViewData["TableId"] = "ImportVoucher";
}
@section HeaderLayout{
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>ImportVoucher</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a asp-page="/DashBoardPage/Index">Home</a></li>
                    <li class="breadcrumb-item active">ImportVoucher</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
}

@section TableTitle{
    <div class="d-flex justify-content-end">
        <a asp-page="Create">Create New</a>
    </div>
}
<table class="table table-bordered table-striped" id="@ViewBag.TableId">
    <thead>
        <tr>
            <th>#</th>
            <th>
                @Html.DisplayNameFor(model => model.PhieuNhap[0].NgayLap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhieuNhap[0].NhanVien.Email)
            </th>
            <th>
                Họ
            </th>
            <th>
                Tên
            </th>
        </tr>

    </thead>
    <tbody>
        @foreach (var item in Model.PhieuNhap)
        {
            <tr>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NgayLap)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NhanVien.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NhanVien.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NhanVien.FirstName)
                </td>
            </tr>
        }
    </tbody>
</table>
